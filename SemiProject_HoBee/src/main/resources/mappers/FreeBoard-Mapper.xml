<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="hobee.semi.project.board.model.mapper.FreeBoardMapper">



<select id="getListCount" resultType="_int">
SELECT COUNT(*)
FROM "BOARD"
WHERE BOARD_DEL_FL = 'N'
AND BOARD_CODE = #{boardCode}
</select>


<select id="selectBoardList" resultType="Board">
SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICKNAME, BOARD_VIEW_COUNT,
(SELECT COUNT(*) FROM "COMMENT" C WHERE C.BOARD_NO = B.BOARD_NO) COMMENT_COUNT,
(SELECT COUNT(*) FROM "BOARD_LIKE" L WHERE L.BOARD_NO = B.BOARD_NO) LIKE_COUNT,
<![CDATA[
CASE
WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60 THEN FLOOR((SYSDATE - B.BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전'
WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 THEN FLOOR((SYSDATE - B.BOARD_WRITE_DATE) * 24 * 60) || '분 전'
WHEN SYSDATE - BOARD_WRITE_DATE < 1 THEN FLOOR((SYSDATE - B.BOARD_WRITE_DATE) * 24) || '시간 전'
ELSE TO_CHAR(B.BOARD_WRITE_DATE, 'YYYY-MM-DD')
END BOARD_WRITE_DATE]]>
FROM "BOARD" B
JOIN "MEMBER" USING(MEMBER_NO)
WHERE BOARD_DEL_FL = 'N'
AND BOARD_CODE = #{boardCode}
ORDER BY BOARD_NO DESC

</select>


<select id="getSearchCount" resultType="_int">
    SELECT COUNT(*)
    FROM "BOARD"
    JOIN "MEMBER" USING(MEMBER_NO)
    WHERE BOARD_DEL_FL = 'N'
    AND BOARD_CODE = #{boardCode}
    <choose>
        <when test='key == "t"'> AND BOARD_TITLE LIKE '%' || #{query} ||'%' </when>
        <when test='key == "c"'> AND BOARD_CONTENT LIKE '%' || #{query} ||'%' </when>
        <when test='key == "tc"'> AND (BOARD_CONTENT LIKE '%' || #{query} ||'%' OR BOARD_TITLE LIKE '%' || #{query} ||'%') </when>
        <when test='key == "w"'> AND MEMBER_NICKNAME LIKE '%' || #{query} ||'%' </when>
    </choose>
</select>


<select id="selectSearchList" resultType="Board">
    SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICKNAME, BOARD_VIEW_COUNT,
    (SELECT COUNT(*) FROM "COMMENT" C WHERE C.BOARD_NO = B.BOARD_NO) COMMENT_COUNT,
    (SELECT COUNT(*) FROM "BOARD_LIKE" L WHERE L.BOARD_NO = B.BOARD_NO) LIKE_COUNT,
    <![CDATA[
    CASE
        WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60 THEN FLOOR((SYSDATE - B.BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전'
        WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 THEN FLOOR((SYSDATE - B.BOARD_WRITE_DATE) * 24 * 60) || '분 전'
        WHEN SYSDATE - BOARD_WRITE_DATE < 1 THEN FLOOR((SYSDATE - B.BOARD_WRITE_DATE) * 24) || '시간 전'
        ELSE TO_CHAR(B.BOARD_WRITE_DATE, 'YYYY-MM-DD')
    END BOARD_WRITE_DATE
    ]]>
    FROM "BOARD" B
    JOIN "MEMBER" USING(MEMBER_NO)
    WHERE BOARD_DEL_FL = 'N'
    AND BOARD_CODE = #{boardCode}
    <choose>
        <when test='key == "t"'> AND BOARD_TITLE LIKE '%' || #{query} ||'%' </when>
        <when test='key == "c"'> AND BOARD_CONTENT LIKE '%' || #{query} ||'%' </when>
        <when test='key == "tc"'> AND (BOARD_CONTENT LIKE '%' || #{query} ||'%' OR BOARD_TITLE LIKE '%' || #{query} ||'%') </when>
        <when test='key == "w"'> AND MEMBER_NICKNAME LIKE '%' || #{query} ||'%' </when>
    </choose>
    ORDER BY BOARD_NO DESC
</select>


<select id="selectFreeBestList" resultType="Board">
SELECT * FROM (
        SELECT BOARD_NO, BOARD_TITLE, 
               (SELECT COUNT(*) FROM "BOARD_LIKE" L 
                WHERE L.BOARD_NO = B.BOARD_NO) AS LIKE_COUNT
        FROM "BOARD" B
        WHERE BOARD_CODE = 3 
        AND BOARD_DEL_FL = 'N'
        ORDER BY LIKE_COUNT DESC
    )
    <![CDATA[ WHERE ROWNUM <= 5 ]]>
</select>

<select id="selectNoticeList" resultType="Board">
    SELECT * FROM (
        SELECT BOARD_NO, BOARD_TITLE,
               TO_CHAR(BOARD_WRITE_DATE, 'YYYY-MM-DD') AS BOARD_WRITE_DATE
        FROM "BOARD"
        WHERE BOARD_CODE = #{noticeBoardCode}
        AND BOARD_DEL_FL = 'N'
        ORDER BY BOARD_NO DESC
    )
    <![CDATA[ WHERE ROWNUM <= 5 ]]>
</select>

	<select id="selectBoardDetail">
		SELECT BOARD_NO, BOARD_TITLE, BOARD_CODE, BOARD_VIEW_COUNT, BOARD_CONTENT,
		MEMBER_NO, MEMBER_NICKNAME, 
		TO_CHAR(BOARD_WRITE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_WRITE_DATE,
		TO_CHAR(BOARD_UPDATE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_UPDATE_DATE,
		
		(SELECT COUNT(*)
		FROM "BOARD_LIKE"
		WHERE BOARD_NO = #{boardNo}) LIKE_COUNT,
		
		(SELECT COUNT(*)
		FROM "BOARD_LIKE"
		WHERE BOARD_NO = #{boardNo}
		AND MEMBER_NO = #{memberNo}) LIKE_CHECK
		
		FROM "BOARD"
		JOIN "MEMBER" USING(MEMBER_NO)
		WHERE BOARD_DEL_FL = 'N'
		AND BOARD_NO = #{boardNo}
	</select>

</mapper>