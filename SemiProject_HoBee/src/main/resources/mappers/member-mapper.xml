<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="hobee.semi.project.member.model.mapper.MemberMapper">
	
		<resultMap type="MemberDTO" id="login_rm">
			<id property="memberNo" column="MEMBER_NO"/>
			
			<collection property="hobbyCode" 
	 					column="MEMBER_NO"
	 					select="selectHobbyList"
	 					javaType="java.util.ArrayList"
	 					ofType="string" />
	 					
		</resultMap>
	
		<select id="selectHobbyList" resultType="string">
			SELECT HOBBY_CODE FROM MEMBER_HOBBY
			WHERE MEMBER_NO = #{memberNo}
		</select>		

		<select id="loginMember" resultMap="login_rm">
			SELECT
			MEMBER_NO,MEMBER_EMAIL,MEMBER_ID,MEMBER_PW,MEMBER_NAME,MEMBER_NICKNAME,
			MEMBER_TEL,MEMBER_ADDRESS,ENROLL_DATE,AUTHOR_LEVEL,MEMBER_DEL_FL, MEMBER_INTRODUCTION, PROFILE_IMG
			FROM "MEMBER"
			WHERE MEMBER_ID = #{memberId}
			AND MEMBER_DEL_FL = 'N'
		</select>
		
		<select id="checkEmail" resultType="_int">
			SELECT COUNT(*)
			FROM "MEMBER"
			WHERE MEMBER_EMAIL = #{memberEmail}
			AND MEMBER_DEL_FL = 'N'
		</select>
		
		<select id="checkId" resultType="_int">
			SELECT COUNT(*)
			FROM "MEMBER"
			WHERE MEMBER_ID = #{memberId}
			AND MEMBER_DEL_FL = 'N'
		</select>
		
		<select id="checkNickname" resultType="_int">
			SELECT COUNT(*)
			FROM "MEMBER"
			WHERE MEMBER_NICKNAME = #{memberNickname}
			AND MEMBER_DEL_FL = 'N'
		</select>
		
		<insert id="signUp" parameterType="MemberDTO">
	    <selectKey order="BEFORE" resultType="_int" keyProperty="memberNo">
	        SELECT SEQ_MEMBER_NO.NEXTVAL FROM DUAL
	    </selectKey>
		    INSERT INTO "MEMBER"
		    VALUES(
		        #{memberNo}, 
		        #{memberId}, 
		        #{memberPw}, 
		        #{memberName}, 
		        #{memberEmail}, 
		        #{memberNickname}, 
		        #{memberTel}, 
		        #{memberAddress}, 
		        DEFAULT, 
		        DEFAULT, 
		        DEFAULT, 
		        #{memberIntroduction},
		        #{profileImg}
	    	)
		</insert>
	
		<insert id="insertMemberHobby">
	    	INSERT INTO "MEMBER_HOBBY" 
	    	(MEMBER_NO, HOBBY_CODE)
	    	VALUES (
				#{memberNo}, 
				#{hobbyCode}
				)
		</insert>
		
		<!--최신 프로필 이미지 조회-->
		<select id="selectLatestProfile" resultType="ProfileDTO">
    	SELECT PROFILE_PATH, PROFILE_RENAME
    	FROM PROFILE_IMG
    	WHERE MEMBER_NO = #{memberNo}
		</select>
		
		<select id="checkName">
			SELECT COUNT(*)
			FROM "MEMBER"
			WHERE MEMBER_EMAIL = #{memberEmail}
			AND MEMBER_DEL_FL = 'N'
			AND MEMBER_NAME = #{memberName}
		</select>
		
		<select id="checkTel">
			SELECT COUNT(*)
			FROM "MEMBER"
			WHERE MEMBER_EMAIL = #{memberEmail}
			AND MEMBER_DEL_FL = 'N'
			AND MEMBER_TEL = #{memberTel}
		</select>
		
		<select id="findId" resultType="string">
		    SELECT MEMBER_ID
		    FROM "MEMBER"
		    WHERE MEMBER_EMAIL = #{memberEmail}
		    AND MEMBER_DEL_FL = 'N'
		</select>
		
		<update id="pwChange">
			UPDATE "MEMBER" SET
    		MEMBER_PW = #{memberPw}
   			WHERE MEMBER_EMAIL = #{memberEmail}
   		 	AND MEMBER_DEL_FL = 'N'
		</update>
		
		
</mapper>
